NL库：用户控制引擎核心数据
1、主要是通过注册事件来进行逻辑，比如：战斗前后、登录\登出等，如果需要触发的点未被开发开发在API中，则会造成一些麻烦。如：当bos的血量达到21%、玩家使用了魔法攻击等。在这些情况下可能不能简单的通过API来实现。需要再研究。
2、NL时间回调函数，单纯看文档并不是非常清晰，但是没有难度，仅需要尝试即可。问题点在于：此处的回调函数是不是与普通的函数相同，亦或是需要名称等特殊限制？需要结合当前已有的脚本进行简单的摸索即可。
3、基本上每一个NL事件设定函数都对应一个回调函数，如此设计是合理的。
4、怀疑可能需要将所有的该类回调函数写在一起。如：TitleChangedCallBack 中可以写function 1、function 2 等。

NLG库：功能指令库
待解释：“NLG庫與其他庫不同的是 其他庫的修改數據指令只是把數據修改，並不把數據更新封包發送給客戶端。”
1、可以使用lua创建一个战斗，并灵活控制战斗信息CreateBattle
2、UpChar 如何定义“关联的玩家”
3、有观看战斗的功能WatchEntry

Char库：
用户操作对象（人物、宠物、道具、NPC等）可以更改/读取对象的内部数据。但不会向客户端发送更新封包。（“不会向客户端发送更新封包”如何理解）
1、lua初始化npc——Init事件，调用NL.CreateNpc对应
2、返回值可以有多个。WalkPreCallBack
3、可以对“对话框”的按钮做自定义处理
4、Char库仍然有事件、回调函数、事件设定函数三种定义方式，需要学习已有脚本再进行制作，应该属于简单操作。
5、对于现有的API有进行二次封装的必要，如查找某玩家身上的某件道具，如果道具名称是唯一的，可使用名称作为参数而非ID
6、NowEvent/EndEvent 需要针对代码再进行研究

Obj库：
1、通过X/Y即可定位玩家/宠物/道具位置。

Battle库：
1、貌似无法获取PK胜负的数据，BattleWin 只是战斗结束时触发，也许可以通过“战斗索引”获取战斗的胜负等数据，需要试验。

SQL库：
1、需要结合实际进行体验，这应该很容易。

参考：
1、所有的常量都是中文，而且是繁体...口味太重了。

待开发内容：
如果可以拿到战斗记录，我想应该可以自己封装很多自己的实现，如bos的血量处于21%类似于这样的需求，如果能通过什么接口能拿到战斗记录就再好不过了，他们这些API可能都是基于战斗记录所做的封装，即使不是，拿到战斗记录对于咱们以后的扩展点也是极有好处的，需要再进行调研。


以上
2015-1-22 针对魔力luaAPI进行的部分分析
lp